spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tmpDB?serverTimezone=Asia/Seoul
    username: #root (본인유저네임)
    password: #5757anaN@@ (본인 비번)
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create # 자동 생성
    properties:
      hibernate:
        format_sql: true # DB로 날리는 쿼리를 보기쉽게함

logging:
  level:
    org.hibernate:
      SQL: debug  # Log level로 쿼리 보기
      type: trace # 쿼리에(?,?,?) 값의 상세 값 보기